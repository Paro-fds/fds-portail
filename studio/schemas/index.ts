// Point d'entrée des schémas — ajouter ici tout nouveau type de document
import { actualiteSchema } from "./actualite";
import { dateCleSchema } from "./dateCle";

export const schemaTypes = [actualiteSchema, dateCleSchema];
